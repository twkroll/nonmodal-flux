# MASTER Prompt — Cross-Domain Application Literature Positioning Audit 0.1

**Authority:** `research/master/cross_domain_result_integration_freeze_0_1.md`  
**Target chat:** `80 – LIT – Literatur & Lernpfad`  
**Scope:** literature positioning only. No new theory, no new numerical execution, no parameter search, no retuning of frozen pilots.

## Task

Perform a targeted novelty/prior-art positioning audit for the two newly frozen application results:

1. `Neuro Pilot 0.1 = NEURO-STRONG`;
2. `Climate/Ocean Pilot 0.1 = CLIM-WEAK`.

The Plasma/D10-ZF P2-A literature audit is already complete and should be used only as cross-domain context, not repeated.

## A. Neuro target result

Use the canonical frozen result:

`research/neuro/neuro_pilot_0_1_execution_results.md`

The frozen Neuro result is a two-source V1/V4 CMC/DCM model with:

- positive model-internal synaptic-filter storage `M`;
- physiologically pre-defined V1-SP -> V4-SS pathway channel
  \[
  Q_{j\to i}^{\rm CORE}=\frac12(A_{j\to i}^\dagger M+MA_{j\to i});
  \]
- rank-two admissible initial preparation generated by two fixed afferent pulses through the same V1-SS input pathway;
- terminal-storage-optimal versus cumulative-pathway-contribution-optimal preparation comparison;
- `NEURO-STRONG`, with neighboring-horizon support at 112 and 224 ms;
- direct interpretation in the two-pulse experimental coordinate basis.

Search specifically for prior art on:

- nonmodal/transient-growth analysis in neural-mass, CMC, DCM, Wilson-Cowan or related cortical population models;
- optimal initial perturbations in neural-mass/network models;
- finite-time quadratic-output optimization in neuroscience;
- pathway-/edge-/connection-specific quadratic objectives;
- optimal stimulation or input design in DCM/CMC and whether it optimizes time-dependent controls rather than frozen initial/preparation coordinates;
- multi-pulse preparation/stimulation geometry and reachable initial-state subspaces;
- network-control approaches comparing state/storage objectives with pathway-specific transfer objectives;
- directed effective-connectivity/pathway contribution optimization;
- any SAME result comparing a positive storage/state objective to a separately optimized signed physiological pathway contribution in the same stable CMC/DCM tangent dynamics.

Important semantic restriction: do not search or report `M` as metabolic brain energy. Use `synaptic-filter storage` / `quadratic storage of neural-mass filter states`.

Important geometry restriction: the frozen admissible cumulative channel minimum is positive on the rank-two preparation space. Do not position the Neuro pilot as demonstrating reachable negative cumulative pathway transfer.

## B. Climate/Ocean target result

Use the canonical frozen result:

`research/climate/climate_ocean_pilot_0_1_execution_results.md`

The frozen Climate result is a stable damped two-layer Phillips-QG pilot with:

- QG perturbation energy `M_K`;
- physically defined signed cumulative meridional eddy heat transport `Q_heat,K`;
- energy-optimal versus positive cumulative-heat-optimal perturbations;
- robust resolution ladder;
- `CLIM-WEAK`;
- at `T/tau_ref=8`, orthogonal optimal modal subspaces `(3,2)` versus `(4,2)` but only
  \[
  \Delta_{\rm heat}=0.04118455338.
  \]

Search specifically for prior art on:

- atmospheric/oceanic QG singular vectors and optimal perturbations;
- energy-norm versus alternative objective/norm singular vectors;
- heat-flux-, eddy-heat-transport-, temperature-flux- or poleward-transport-maximizing perturbations;
- finite-time signed flux optimization in baroclinic/QG systems;
- optimal perturbations for momentum, heat, scalar or transport objectives;
- objective-dependent mode/support switches;
- cases where optimizer/subspace angle is large but objective-value penalty is small;
- any SAME result directly comparing energy-optimal and signed cumulative eddy-heat-transport-optimal initial perturbations in the same stable two-layer QG tangent system.

Do not inflate the frozen `CLIM-WEAK` result into a strong replication. The specific literature question is whether its robust geometry-versus-performance contrast is already standard or still useful as an application/methodological case.

## C. Classification rule

For every especially relevant paper classify the relationship as exactly one of:

- `SAME` — essentially the same physical model/objective comparison/result;
- `CLOSE` — same principle and closely adjacent model/objective, but not the full frozen result;
- `RELATED` — relevant method/domain literature without the same comparison;
- `DIFFERENT` — superficially similar but solves a materially different problem.

Do not treat absence from a targeted search as proof of novelty. Use `to our knowledge` for any surviving novelty-sensitive statement.

## D. Required output

Produce one canonical Markdown report:

`research/literature/cross_domain_application_literature_positioning_audit_0_1.md`

and update:

`research/literature/STATUS.md`

The report must contain:

1. search scope and databases/search strategy;
2. Neuro literature table with SAME/CLOSE/RELATED/DIFFERENT classification;
3. Climate/Ocean literature table with the same classification;
4. explicit strongest prior-art overlaps;
5. explicit novelty claims that must be DEMOTED;
6. application-specific claims that survive, if any;
7. safe abstract/introduction formulations for each domain;
8. whether the integrated three-domain story remains publication-positionable;
9. recommended next MASTER action;
10. exact STOP.

If a SAME hit substantially collapses a claim, report it directly. Do not rescue the claim by changing the frozen model, objective, horizon or interpretation.

Commit the report and updated status. Then set

`EXECUTION COMPLETE — RETURN TO MASTER FOR INTEGRATION`

and STOP.